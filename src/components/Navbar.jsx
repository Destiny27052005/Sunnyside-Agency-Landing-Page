import Logo from "../assets/image/logo.svg"

function Navbar() {
    return (
        <nav>
            <img src={Logo} alt="Sunnyside Logo" />
            
        </nav>
    );
}

export default Navbar;